import { LoginUser } from "../users/login_user";
import { Role } from "../users/Role";

export class Admin extends LoginUser{
    // userId!: number;
    // userName!: string;
    // phoneNo!: string;
    // emailID!: string;
    // password!: string;
    // age!: number;
    // gender!: string;    
    // roles!: Role[];
}